<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
</head>
<body>
    <% if (kindOfDay === "sábado" || kindOfDay === "domingo") { %>
        <h1 style="color: purple"><%= salute%>, hoje é <%= kindOfDay %> !</h1>
    <% } else { %>
        <h1 style="color: blue"><%= salute%>, hoje é <%= kindOfDay %> !</h1>
    <% } %>
</body>
</html>